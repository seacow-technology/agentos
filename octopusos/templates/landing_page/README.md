# AgentOS Landing Page

This landing page was automatically generated by AgentOS using the Mode Pipeline system.

## Overview

This is a demo landing page showcasing AgentOS - an OS-level governance layer for AI execution.

## Files

- `index.html` - Main HTML structure with 5 sections (Hero, Features, Architecture, Use Cases, Footer)
- `style.css` - Responsive CSS styles with modern design
- `README.md` - This file

## Local Development

To view this landing page locally:

1. **Simple HTTP Server (Python)**:
   ```bash
   python3 -m http.server 8000
   ```
   Then open http://localhost:8000

2. **Using Node.js**:
   ```bash
   npx serve .
   ```

3. **Direct File Open**:
   Simply open `index.html` in your browser (some features may not work without a server)

## Sections

1. **Hero** - Main introduction with CTA buttons
2. **Features** - 4 core feature cards (Mode System, Audit Trail, Isolation, Rollback)
3. **Architecture** - Visual diagram of the system architecture
4. **Use Cases** - 3 real-world use case examples
5. **Footer** - Links and copyright information

## Responsive Design

This landing page is fully responsive and works on:
- Desktop (1200px+)
- Tablet (768px - 1200px)
- Mobile (< 768px)

## AgentOS Execution Evidence

This page was created through the following pipeline:

- **Mode Selection**: `planning → implementation`
- **Execution Stages**: 6 commits (init skeleton → hero → features → architecture → use cases → footer)
- **Audit Trail**: See `outputs/pipeline_xxx/audit/run_tape.jsonl` for complete execution log
- **Git History**: Each section was added in a separate commit for clear rollback capability

## Rollback

To rollback to any previous step:

```bash
# View commit history
git log --oneline

# Rollback to a specific commit
git reset --hard <commit-hash>

# Example: Rollback to just after features section
git reset --hard HEAD~3
```

## Customization

To customize this page:

1. Edit `index.html` to change content
2. Edit `style.css` to change styling
3. Add images to an `assets/` directory if needed

## License

MIT License - See LICENSE file in the AgentOS repository.

---

**Generated by**: AgentOS v1.0  
**Date**: 2026-01-26  
**Pipeline**: Mode Pipeline Demo
