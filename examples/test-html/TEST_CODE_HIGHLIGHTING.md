# 代码语法高亮功能测试指南

## ✅ 实施完成

代码语法高亮功能已完全实施并部署到服务器。

**实施内容**：
- ✅ 集成 PrismJS (CDN版本)
- ✅ 添加语言规范化函数
- ✅ 修改代码块渲染输出 Prism 结构
- ✅ 添加自动高亮函数
- ✅ 调整 CSS 样式（Tomorrow Night 主题）
- ✅ 服务器已重启

---

## 🧪 测试方法

### 测试 1: JavaScript 代码高亮

在 Chat 页面发送：

```
请写一个 JavaScript 函数，实现防抖功能
```

期望 AI 回复包含：

````javascript
```javascript
function debounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
}

// 使用示例
const debouncedFunction = debounce(() => {
    console.log('Hello!');
}, 300);
```
````

**验证点**：
- [ ] 关键字 `function`, `let`, `return` 显示为紫色
- [ ] 字符串 `'Hello!'` 显示为绿色
- [ ] 注释 `// 使用示例` 显示为灰色斜体
- [ ] 数字 `300` 显示为红色
- [ ] 函数名 `debounce` 显示为蓝色
- [ ] 代码背景为深灰色 (#1d1f21)

### 测试 2: Python 代码高亮

发送：

```
写一个 Python 的快速排序算法
```

期望回复：

````python
```python
def quicksort(arr):
    if len(arr) <= 1:
        return arr

    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]

    return quicksort(left) + middle + quicksort(right)

# 测试
print(quicksort([3, 6, 8, 10, 1, 2, 1]))
```
````

**验证点**：
- [ ] 关键字 `def`, `if`, `return`, `for`, `in` 显示为紫色
- [ ] 函数名 `quicksort`, `len`, `print` 显示为蓝色
- [ ] 注释 `# 测试` 显示为灰色斜体
- [ ] 运算符 `+`, `==`, `<`, `>` 显示为橙色
- [ ] 列表 `[]` 括号正确高亮

### 测试 3: HTML 代码高亮 + Preview

发送：

```
创建一个 HTML 页面，包含一个表单
```

期望回复包含 HTML 代码块。

**验证点**：
- [ ] HTML 标签 `<html>`, `<body>`, `<form>` 显示为红色
- [ ] 属性名 `class`, `id`, `action` 显示为蓝色
- [ ] 属性值字符串显示为绿色
- [ ] Preview 按钮正常显示
- [ ] 点击 Preview 可以预览 HTML
- [ ] Copy 按钮可以复制原始 HTML 文本

### 测试 4: CSS 代码高亮

发送：

```
写一个 CSS 动画
```

期望回复：

````css
```css
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.fade-in {
    animation: fadeIn 0.5s ease-out;
}
```
````

**验证点**：
- [ ] `@keyframes` 显示为紫色
- [ ] 选择器 `.fade-in` 显示为合适的颜色
- [ ] 属性名 `opacity`, `transform`, `animation` 显示为蓝色
- [ ] 属性值 `0`, `1`, `0.5s`, `ease-out` 正确高亮
- [ ] 单位 `px`, `s` 显示正确

### 测试 5: JSON 代码高亮

发送：

```
给我一个 JSON 配置示例
```

期望回复：

````json
```json
{
    "name": "AgentOS",
    "version": "0.3.0",
    "config": {
        "debug": true,
        "port": 8080,
        "features": ["chat", "preview", "highlight"]
    }
}
```
````

**验证点**：
- [ ] 键名（字符串） `"name"`, `"version"` 显示为绿色
- [ ] 字符串值 `"AgentOS"` 显示为绿色
- [ ] 布尔值 `true` 显示为红色
- [ ] 数字 `8080` 显示为红色
- [ ] 括号 `{}`, `[]` 正确高亮

### 测试 6: Bash/Shell 代码高亮

发送：

```
写一个 shell 脚本来备份文件
```

期望回复：

````bash
```bash
#!/bin/bash

SOURCE="/path/to/source"
DEST="/path/to/backup"

# 创建备份
tar -czf "${DEST}/backup-$(date +%Y%m%d).tar.gz" "${SOURCE}"

echo "Backup completed!"
```
````

**验证点**：
- [ ] Shebang `#!/bin/bash` 显示为注释色
- [ ] 变量 `$SOURCE`, `$DEST` 正确高亮
- [ ] 命令 `tar`, `date`, `echo` 显示正确
- [ ] 注释 `# 创建备份` 显示为灰色斜体
- [ ] 字符串 `"Backup completed!"` 显示为绿色

### 测试 7: TypeScript 代码高亮

发送：

```
写一个 TypeScript 接口定义
```

期望回复：

````typescript
```typescript
interface User {
    id: number;
    name: string;
    email?: string;
    roles: Array<string>;
}

const user: User = {
    id: 1,
    name: "Alice",
    roles: ["admin", "user"]
};
```
````

**验证点**：
- [ ] 关键字 `interface`, `const` 显示为紫色
- [ ] 类型注解 `number`, `string`, `Array` 显示正确
- [ ] 可选属性 `?` 符号显示
- [ ] 对象字面量正确高亮

### 测试 8: 多代码块混合

发送包含多个不同语言代码块的消息：

```
展示 HTML, CSS 和 JavaScript 的组合示例
```

**验证点**：
- [ ] 每个代码块独立高亮
- [ ] HTML 代码块有 Preview 按钮
- [ ] CSS 和 JS 代码块没有 Preview 按钮
- [ ] 所有代码块都有 Copy 按钮
- [ ] 高亮颜色正确且一致

### 测试 9: 无语言标识的代码块

发送一个没有语言标识的代码块：

````
```
function test() {
    console.log("test");
}
```
````

**验证点**：
- [ ] 使用 fallback 语言（clike）
- [ ] 基本语法高亮仍然有效
- [ ] 不显示 Preview 按钮
- [ ] Copy 按钮正常工作

### 测试 10: 流式消息高亮

1. 发送需要 AI 生成大量代码的请求
2. 观察流式输出过程

**验证点**：
- [ ] 流式过程显示纯文本（性能考虑）
- [ ] `message.end` 后代码块自动高亮
- [ ] 高亮应用正确且快速（<100ms）
- [ ] 无闪烁或跳动

### 测试 11: 历史消息加载

1. 刷新页面
2. 查看历史消息

**验证点**：
- [ ] 历史消息中的代码块自动高亮
- [ ] 颜色与新消息一致
- [ ] Preview 和 Copy 功能正常

### 测试 12: Copy 功能验证

1. 点击任意代码块的 Copy 按钮
2. 粘贴到文本编辑器

**验证点**：
- [ ] 复制的是原始文本（不是高亮后的 HTML）
- [ ] 缩进和换行保持正确
- [ ] 没有多余的空格或字符
- [ ] Copy 按钮显示 "Copied!" 反馈

### 测试 13: Preview 兼容性

1. 找到 HTML 代码块
2. 点击 Preview 按钮

**验证点**：
- [ ] Preview 正常打开
- [ ] HTML 渲染正确
- [ ] 交互功能正常（按钮、表单等）
- [ ] 高亮不影响 Preview 功能

---

## 🎨 视觉验证

### 颜色方案（Tomorrow Night）

检查各种 token 的颜色是否正确：

| Token 类型 | 颜色 | 示例 |
|-----------|------|------|
| 背景 | `#1d1f21` | 代码块背景 |
| 默认文本 | `#c5c8c6` | 普通代码 |
| 注释 | `#969896` | `// comment` |
| 关键字 | `#b294bb` | `function`, `const`, `def` |
| 字符串 | `#b5bd68` | `"string"`, `'string'` |
| 函数名 | `#81a2be` | `myFunction()` |
| 数字/布尔 | `#cc6666` | `123`, `true`, `false` |
| 运算符 | `#de935f` | `+`, `-`, `=`, `==` |
| 正则表达式 | `#8abeb7` | `/regex/` |

### UI 一致性

- [ ] 代码块卡片样式与之前一致
- [ ] 头部（语言标识 + 按钮）保持不变
- [ ] 按钮样式和位置正确
- [ ] Hover 效果正常
- [ ] 圆角和阴影正确

---

## 🔧 开发者验证

### 浏览器 DevTools

1. 打开浏览器开发者工具
2. 查看 Console 输出

**验证点**：
- [ ] 无 JavaScript 错误
- [ ] 看到 "Syntax highlighting applied" 日志
- [ ] PrismJS 已加载：`console.log(window.Prism)`
- [ ] 各语言组件已加载

### 网络请求

1. 打开 Network 面板
2. 刷新页面

**验证点**：
- [ ] PrismJS CDN 资源正常加载
- [ ] CSS 文件加载正常
- [ ] JS 文件加载正常
- [ ] 无 404 错误

### 性能测试

1. 打开 Performance 面板
2. 发送包含代码块的消息
3. 记录性能

**验证点**：
- [ ] `message.end` 到高亮完成 < 100ms
- [ ] 无明显卡顿
- [ ] 内存使用正常
- [ ] CPU 占用合理

---

## ✅ 验收标准

### 必须通过

- [ ] 所有主流语言高亮正确（JS/TS/Python/HTML/CSS/JSON/Bash）
- [ ] 颜色与 Tomorrow Night 主题一致
- [ ] Preview 功能不受影响
- [ ] Copy 功能复制原始文本
- [ ] 流式消息正确高亮
- [ ] 历史消息正确高亮
- [ ] 无 JavaScript 错误
- [ ] 性能无明显下降

### 可选增强（未来）

- [ ] 添加行号显示
- [ ] 添加代码折叠
- [ ] 添加全屏查看
- [ ] 添加更多语言支持
- [ ] 下载为本地文件（替代 CDN）

---

## 🐛 问题排查

### 问题 1: 代码没有高亮

**可能原因**：
- PrismJS 未加载
- 语言组件未加载
- `highlightCodeBlocks` 未调用

**排查步骤**：
1. 检查 Console 是否有错误
2. 验证 `window.Prism` 存在
3. 检查代码块是否有 `language-*` class
4. 查看网络请求是否成功

### 问题 2: 颜色不对

**可能原因**：
- CSS 未加载
- 样式被覆盖

**排查步骤**：
1. 检查 `prism-tomorrow.css` 是否加载
2. 检查 `components.css` 的覆盖样式
3. 使用 DevTools 检查元素样式

### 问题 3: Preview 不工作

**可能原因**：
- Copy 函数使用了错误的选择器
- HTML 被转义

**排查步骤**：
1. 检查是否使用 `textContent` 而不是 `innerHTML`
2. 验证代码块结构正确
3. 查看 Console 错误

### 问题 4: 性能慢

**可能原因**：
- 全局高亮（未使用 `highlightAllUnder`）
- 重复高亮

**排查步骤**：
1. 确认使用 `Prism.highlightAllUnder(element)`
2. 确认只在 `message.end` 调用
3. 使用 Performance 工具分析

---

## 📊 测试报告模板

```
测试日期: ____________________
测试人员: ____________________
浏览器: ____________________
测试结果: ☐ 通过 ☐ 失败

测试项:
☐ JavaScript 高亮
☐ Python 高亮
☐ HTML 高亮
☐ CSS 高亮
☐ JSON 高亮
☐ Bash 高亮
☐ TypeScript 高亮
☐ 多代码块混合
☐ 流式消息高亮
☐ 历史消息高亮
☐ Copy 功能
☐ Preview 功能
☐ 性能测试

问题记录:
1. ____________________
2. ____________________
3. ____________________

建议改进:
1. ____________________
2. ____________________
```

---

## 🚀 部署状态

- ✅ 代码已实施
- ✅ 服务器已重启
- ✅ 版本号已更新（v22）
- ✅ 准备测试

**测试 URL**: http://127.0.0.1:8080

**快速测试**：
1. 访问 Chat 页面
2. 发送: `"写一个 JavaScript 函数"`
3. 验证代码高亮效果

---

**实施完成时间**: 2026-01-28
**功能状态**: ✅ 准备测试
